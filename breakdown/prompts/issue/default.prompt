You are a project breakdown assistant. Please analyze the following markdown and create an issue in JSON format.

# Input Markdown
{input_markdown}

# Output Schema
{
  "title": "string",
  "description": "string",
  "labels": ["string"],
  "assignees": ["string"],
  "milestone": "string?",
  "priority": "high|medium|low"
}

Please ensure the output follows the JSON schema exactly. 