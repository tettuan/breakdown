# Test Helper Application Configuration for Unit Tests
# SINGLE SOURCE OF TRUTH
working_dir: "tests/fixtures"
app_prompt:
  base_dir: "prompts"

app_schema:
  base_dir: "schema"

features:
  debugMode: true
  strictValidation: false
  enableCache: false

logging:
  level: "info"

# Test environment configuration
testEnvironment:
  parallel: false
  timeout: 30000
  retries: 1
