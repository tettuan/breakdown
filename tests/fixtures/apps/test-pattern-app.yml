# パターンテスト用アプリケーション設定
application:
  name: "Breakdown Test Pattern"
  version: "1.0.0"

paths:
  workDir: "./tests/fixtures"
  resourceDir: "./tests/fixtures"
  promptDir: "./prompts"
  schemaDir: "./schema"

settings:
  debug: false
  logLevel: "warn"

# パターンマッチングテスト設定
patternTests:
  enabled: true
  strictMode: false
