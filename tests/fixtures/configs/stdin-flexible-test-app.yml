# STDIN Flexible Test Application Configuration for E2E Testing
# Test configuration for STDIN processing integration tests with flexible patterns

working_dir: "tests/fixtures"
app_prompt:
  base_dir: "prompts"
app_schema:
  base_dir: ".agent/climpt/schema"
params:
  two:
    directiveType:
      pattern: "^(to|summary|defect|to#1|summary_v2|defect-fix|to_all|web#search|db_query|api-call|data#extract)$"
    layerType:
      pattern: "^(project|issue|task|bugs|project#1|issue_v2|task-new|bugs_critical|module#api|service_db|component-ui|layer#data)$"
