/**
 * @fileoverview Two params stdin processor re-export for tests
 * 
 * This module re-exports two params stdin processor for tests
 * 
 * @module tests/lib/cli/processors/two_params_stdin_processor
 */

// Re-export from main lib
export * from "../../../../lib/cli/processors/two_params_stdin_processor.ts";
