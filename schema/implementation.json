{
  "type": "object",
  "properties": {
    "input_text_file": {
      "type": "string",
      "description": "Content of the input file to be analyzed"
    },
    "input_text": {
      "type": "string",
      "description": "Direct text input to be analyzed"
    },
    "destination_path": {
      "type": "string",
      "description": "Path where the analysis report will be written"
    }
  },
  "required": ["destination_path"]
}
